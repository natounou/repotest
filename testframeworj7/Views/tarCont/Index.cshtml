@model IEnumerable<testframeworj7.Models.Tarifs>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.datet)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.prix)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.typedechambres)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.datet)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.prix)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.typedechambres.typeID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.tarID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.tarID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.tarID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
